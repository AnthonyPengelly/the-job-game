{
  "name": "the-job",
  "version": "0.0.0",
  "private": true,
  "description": "The Job — offline co-op heist party game. GM-facing SPA (React + Vite + TS).",
  "type": "module",
  "scripts": {
    "comment:gates": "check:full, sim:check and content:validate start as no-op stubs so the pipeline can bootstrap. Epic E0 replaces check:full; E1 replaces sim:check; E7/E10 replace content:validate. Do NOT leave them as stubs once the relevant epic lands.",
    "dev": "echo 'E0 wires Vite dev server' && exit 0",
    "build": "echo 'E0 wires Vite build' && exit 0",
    "typecheck": "echo 'stub: E0 wires tsc --noEmit' && exit 0",
    "lint": "echo 'stub: E0 wires eslint' && exit 0",
    "test": "echo 'stub: E0 wires vitest' && exit 0",
    "check": "echo 'stub: fast gate (type+lint+unit). E0 wires this.' && exit 0",
    "check:full": "echo 'stub: full gate. E0 wires this (type+lint+unit+sensors).' && exit 0",
    "sim:check": "echo 'stub: balance harness. E1 wires this (Monte Carlo asserts design targets).' && exit 0",
    "content:validate": "echo 'stub: preset/content schema validation. E7/E10 wire this.' && exit 0",
    "agent:preflight": "./scripts/agents/preflight.sh",
    "agent:orchestrate": "./scripts/agents/orchestrate.sh",
    "agent:epic": "./scripts/agents/run-epic.sh"
  }
}
